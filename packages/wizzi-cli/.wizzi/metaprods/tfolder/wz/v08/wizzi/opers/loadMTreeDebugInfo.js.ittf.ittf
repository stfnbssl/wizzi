$group

    $ 
        var openPar = '(';

    function loadMTreeDebugInfo
        param ittfDocumentUri
        param context
        param callback

        _ createWizziFactory
            {
            f_cb${openPar} wf )
                _ wf.loadMTreeDebugInfo
                    @ ittfDocumentUri
                    @ context
                    @ callback