$group

    $
        var items = [
            'catch_reject',
            ];

    $foreach item in items
        $file __dot__wizzi/t/v08/ts/${item}.ittf.ittf
            ${"${'$'}"}group
                ${'$'}include wz/v08/ts/tfolder/${item}.ts