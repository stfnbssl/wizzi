v08/artifacts/statements-writer(function)

	$append imports
            
	v08/artifacts/statement( xfunction
        var name = model.wzName.trim()
        _ ctx.w('def ' + name + '()')
        _ mainWriter.genItems
            @ model.statements
			@ ctx
            {
                @ indent true
			@ callback

	v08/artifacts/statement( xreturn
        var name = model.wzName.trim()
        _ ctx.w('return ' + name)
        r_cb()