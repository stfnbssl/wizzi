/*
    artifact generator: C:\My\wizzi\stfnbssl\wizzi\node_modules\wizzi-js\lib\artifacts\js\module\gen\main.js
    package: wizzi-js@0.7.8
    primary source IttfDocument: C:\My\wizzi\stfnbssl\wizzi\packages\wizzi-lab\lib\artifacts\rdbms\jsonmodel\gen\ittf\jsonmodel.js.ittf
    utc time: Sat, 30 Jul 2022 16:00:35 GMT
*/
'use strict';

//

var artifactcontextmodelPersister = require('../persisters/artifactcontextmodel');

var md = module.exports = {};

md.getArtifactcontextmodels = function(userName, projectName, schemaName, artifactName, modelSchema, callback) {
    artifactcontextmodelPersister.getArtifactcontextmodels(userName, projectName, schemaName, artifactName, modelSchema, callback)
}
;

md.getArtifactcontextmodel = function(userName, projectName, schemaName, artifactName, modelSchema, callback) {
    
    // Persister.get must give back a NotFoundError if item does not exists
    
    artifactcontextmodelPersister.get(userName, projectName, schemaName, artifactName, modelSchema, callback)
}
;

md.insertArtifactcontextmodel = function(userName, projectName, schemaName, artifactName, modelSchema, callback) {
    artifactcontextmodelPersister.exists(userName, projectName, schemaName, artifactName, modelSchema, function(err, result) {
        if (err) {
            return callback(err);
        }
        if (result) {
            return callback(new errors.AlreadyExistsError('artifactcontextmodel', utils.buildResourcePath(userName, projectName, schemaName, artifactName, modelSchema)));
        }
        var newItem = {
            type: 'artifactcontextmodel', 
            userName: null, 
            projectName: null, 
            schemaName: null, 
            artifactName: null, 
            modelSchema: null
         };
        artifactcontextmodelPersister.putArtifactcontextmodel(newItem, function(err, result) {
            if (err) {
                return callback(err);
            }
            //
            // TODO add to oneTable ?
            // TODO or always make the oneTable api call this insert ?
            //
            callback(null, result);
        })
    })
}
;

.prototype.updateArtifactcontextmodel = function(item, callback) {
    artifactcontextmodelPersister.put(item, callback)
}

.prototype.deleteArtifactcontextmodel = function(item, callback) {
    artifactcontextmodelPersister.delete(item, callback)
}

