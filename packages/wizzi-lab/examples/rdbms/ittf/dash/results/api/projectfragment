/*
    artifact generator: C:\My\wizzi\stfnbssl\wizzi\node_modules\wizzi-js\lib\artifacts\js\module\gen\main.js
    package: wizzi-js@0.7.8
    primary source IttfDocument: C:\My\wizzi\stfnbssl\wizzi\packages\wizzi-lab\lib\artifacts\rdbms\jsonmodel\gen\ittf\jsonmodel.js.ittf
    utc time: Sat, 30 Jul 2022 16:00:35 GMT
*/
'use strict';

//

var projectfragmentPersister = require('../persisters/projectfragment');

var md = module.exports = {};

md.getProjectfragments = function(userName, projectName, fragmentName, callback) {
    projectfragmentPersister.getProjectfragments(userName, projectName, fragmentName, callback)
}
;

md.getProjectfragment = function(userName, projectName, fragmentName, callback) {
    
    // Persister.get must give back a NotFoundError if item does not exists
    
    projectfragmentPersister.get(userName, projectName, fragmentName, callback)
}
;

md.insertProjectfragment = function(userName, projectName, fragmentName, callback) {
    projectfragmentPersister.exists(userName, projectName, fragmentName, function(err, result) {
        if (err) {
            return callback(err);
        }
        if (result) {
            return callback(new errors.AlreadyExistsError('projectfragment', utils.buildResourcePath(userName, projectName, fragmentName)));
        }
        var newItem = {
            type: 'projectfragment', 
            userName: null, 
            projectName: null, 
            fragmentName: null
         };
        projectfragmentPersister.putProjectfragment(newItem, function(err, result) {
            if (err) {
                return callback(err);
            }
            //
            // TODO add to oneTable ?
            // TODO or always make the oneTable api call this insert ?
            //
            callback(null, result);
        })
    })
}
;

.prototype.updateProjectfragment = function(item, callback) {
    projectfragmentPersister.put(item, callback)
}

.prototype.deleteProjectfragment = function(item, callback) {
    projectfragmentPersister.delete(item, callback)
}

