/*
    artifact generator: C:\My\wizzi\stfnbssl\wizzi\node_modules\wizzi-js\lib\artifacts\js\module\gen\main.js
    package: wizzi-js@0.7.8
    primary source IttfDocument: C:\My\wizzi\stfnbssl\wizzi\packages\wizzi-lab\lib\artifacts\rdbms\jsonmodel\gen\ittf\jsonmodel.js.ittf
    utc time: Sat, 30 Jul 2022 16:00:35 GMT
*/
'use strict';

//

var schemaPersister = require('../persisters/schema');

var md = module.exports = {};

md.getSchemas = function(userName, projectName, schemaName, callback) {
    schemaPersister.getSchemas(userName, projectName, schemaName, callback)
}
;

md.getSchema = function(userName, projectName, schemaName, callback) {
    
    // Persister.get must give back a NotFoundError if item does not exists
    
    schemaPersister.get(userName, projectName, schemaName, callback)
}
;

md.insertSchema = function(userName, projectName, schemaName, callback) {
    schemaPersister.exists(userName, projectName, schemaName, function(err, result) {
        if (err) {
            return callback(err);
        }
        if (result) {
            return callback(new errors.AlreadyExistsError('schema', utils.buildResourcePath(userName, projectName, schemaName)));
        }
        var newItem = {
            type: 'schema', 
            userName: null, 
            projectName: null, 
            schemaName: null, 
            schemaFragments: [], 
            schemaContextModels: [], 
            schemaContextFragments: [], 
            artifacts: []
         };
        schemaPersister.putSchema(newItem, function(err, result) {
            if (err) {
                return callback(err);
            }
            //
            // TODO add to oneTable ?
            // TODO or always make the oneTable api call this insert ?
            //
            callback(null, result);
        })
    })
}
;

.prototype.updateSchema = function(item, callback) {
    schemaPersister.put(item, callback)
}

.prototype.deleteSchema = function(item, callback) {
    schemaPersister.delete(item, callback)
}

// TODO r.manyTable().keyColumns() - table.keyColumns()
md.getSchemaSchemafragment = function(userName, projectName, schemaName, fragmentName, callback) {
    
    md.getSchema(userName, projectName, schemaName, function(err, result) {
        if (err) {
            return callback(err);
        }
        var ret = util.aFind(result.schemaFragments, {
            'userName': userName, 
            'projectName': projectName, 
            'schemaName': schemaName, 
            'fragmentName': fragmentName
         });
        if (ret) {
            return callback(null, ret);
        }
        else {
            return callback(new errors.NotFoundError(utils.buildResourceName('schema', 'schemafragment'), utils.buildResourcePath(userName, projectName, schemaName, fragmentName)));
        }
    })
}
;

md.addSchemaSchemafragment = function(userName, projectName, schemaName, fragmentName, callback) {
    
    md.getSchema(userName, projectName, schemaName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('schema', 'schemafragment'), utils.buildResourcePath(userName, projectName, schemaName, fragmentName)));
        }
        
        _.addFragment(result, fragmentSchema, fragmentName)
        
        persisterSchema.put(userName, projectName, schemaName, result, callback)
        
    })
}
;

md.removeSchemaSchemafragment = function(userName, projectName, schemaName, fragmentName, callback) {
    
    md.getSchema(userName, projectName, schemaName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('schema', 'schemafragment'), utils.buildResourcePath(userName, projectName, schemaName, fragmentName)));
        }
        
        _.removeFragment(result, fragmentSchema, fragmentName)
        
        persisterSchema.put(userName, projectName, schemaName, result, callback)
        
    })
}
;

// TODO r.manyTable().keyColumns() - table.keyColumns()
md.getSchemaSchemacontextmodel = function(userName, projectName, schemaName, modelSchema, modelName, callback) {
    
    md.getSchema(userName, projectName, schemaName, function(err, result) {
        if (err) {
            return callback(err);
        }
        var ret = util.aFind(result.schemaContextModels, {
            'userName': userName, 
            'projectName': projectName, 
            'schemaName': schemaName, 
            'modelSchema': modelSchema, 
            'modelName': modelName
         });
        if (ret) {
            return callback(null, ret);
        }
        else {
            return callback(new errors.NotFoundError(utils.buildResourceName('schema', 'schemacontextmodel'), utils.buildResourcePath(userName, projectName, schemaName, modelSchema, modelName)));
        }
    })
}
;

md.addSchemaSchemacontextmodel = function(userName, projectName, schemaName, modelSchema, modelName, callback) {
    
    md.getSchema(userName, projectName, schemaName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('schema', 'schemacontextmodel'), utils.buildResourcePath(userName, projectName, schemaName, modelSchema, modelName)));
        }
        
        _.addFragment(result, fragmentSchema, fragmentName)
        
        persisterSchema.put(userName, projectName, schemaName, result, callback)
        
    })
}
;

md.removeSchemaSchemacontextmodel = function(userName, projectName, schemaName, modelSchema, modelName, callback) {
    
    md.getSchema(userName, projectName, schemaName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('schema', 'schemacontextmodel'), utils.buildResourcePath(userName, projectName, schemaName, modelSchema, modelName)));
        }
        
        _.removeFragment(result, fragmentSchema, fragmentName)
        
        persisterSchema.put(userName, projectName, schemaName, result, callback)
        
    })
}
;

// TODO r.manyTable().keyColumns() - table.keyColumns()
md.getSchemaSchemacontextfragment = function(userName, projectName, schemaName, callback) {
    
    md.getSchema(userName, projectName, schemaName, function(err, result) {
        if (err) {
            return callback(err);
        }
        var ret = util.aFind(result.schemaContextFragments, {
            'userName': userName, 
            'projectName': projectName, 
            'schemaName': schemaName
         });
        if (ret) {
            return callback(null, ret);
        }
        else {
            return callback(new errors.NotFoundError(utils.buildResourceName('schema', 'schemacontextfragment'), utils.buildResourcePath(userName, projectName, schemaName)));
        }
    })
}
;

md.addSchemaSchemacontextfragment = function(userName, projectName, schemaName, callback) {
    
    md.getSchema(userName, projectName, schemaName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('schema', 'schemacontextfragment'), utils.buildResourcePath(userName, projectName, schemaName)));
        }
        
        _.addFragment(result, fragmentSchema, fragmentName)
        
        persisterSchema.put(userName, projectName, schemaName, result, callback)
        
    })
}
;

md.removeSchemaSchemacontextfragment = function(userName, projectName, schemaName, callback) {
    
    md.getSchema(userName, projectName, schemaName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('schema', 'schemacontextfragment'), utils.buildResourcePath(userName, projectName, schemaName)));
        }
        
        _.removeFragment(result, fragmentSchema, fragmentName)
        
        persisterSchema.put(userName, projectName, schemaName, result, callback)
        
    })
}
;

// TODO r.manyTable().keyColumns() - table.keyColumns()
md.getSchemaArtifact = function(userName, projectName, schemaName, artifactName, callback) {
    
    md.getSchema(userName, projectName, schemaName, function(err, result) {
        if (err) {
            return callback(err);
        }
        var ret = util.aFind(result.artifacts, {
            'userName': userName, 
            'projectName': projectName, 
            'schemaName': schemaName, 
            'artifactName': artifactName
         });
        if (ret) {
            return callback(null, ret);
        }
        else {
            return callback(new errors.NotFoundError(utils.buildResourceName('schema', 'artifact'), utils.buildResourcePath(userName, projectName, schemaName, artifactName)));
        }
    })
}
;

md.addSchemaArtifact = function(userName, projectName, schemaName, artifactName, callback) {
    
    md.getSchema(userName, projectName, schemaName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('schema', 'artifact'), utils.buildResourcePath(userName, projectName, schemaName, artifactName)));
        }
        
        _.addFragment(result, fragmentSchema, fragmentName)
        
        persisterSchema.put(userName, projectName, schemaName, result, callback)
        
    })
}
;

md.removeSchemaArtifact = function(userName, projectName, schemaName, artifactName, callback) {
    
    md.getSchema(userName, projectName, schemaName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('schema', 'artifact'), utils.buildResourcePath(userName, projectName, schemaName, artifactName)));
        }
        
        _.removeFragment(result, fragmentSchema, fragmentName)
        
        persisterSchema.put(userName, projectName, schemaName, result, callback)
        
    })
}
;

