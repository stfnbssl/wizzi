/*
    artifact generator: C:\My\wizzi\stfnbssl\wizzi\node_modules\wizzi-js\lib\artifacts\js\module\gen\main.js
    package: wizzi-js@0.7.8
    primary source IttfDocument: C:\My\wizzi\stfnbssl\wizzi\packages\wizzi-lab\lib\artifacts\rdbms\jsonmodel\gen\ittf\jsonmodel.js.ittf
    utc time: Sat, 30 Jul 2022 16:00:35 GMT
*/
'use strict';

//

var schemacontextmodelPersister = require('../persisters/schemacontextmodel');

var md = module.exports = {};

md.getSchemacontextmodels = function(userName, projectName, schemaName, modelSchema, modelName, callback) {
    schemacontextmodelPersister.getSchemacontextmodels(userName, projectName, schemaName, modelSchema, modelName, callback)
}
;

md.getSchemacontextmodel = function(userName, projectName, schemaName, modelSchema, modelName, callback) {
    
    // Persister.get must give back a NotFoundError if item does not exists
    
    schemacontextmodelPersister.get(userName, projectName, schemaName, modelSchema, modelName, callback)
}
;

md.insertSchemacontextmodel = function(userName, projectName, schemaName, modelSchema, modelName, callback) {
    schemacontextmodelPersister.exists(userName, projectName, schemaName, modelSchema, modelName, function(err, result) {
        if (err) {
            return callback(err);
        }
        if (result) {
            return callback(new errors.AlreadyExistsError('schemacontextmodel', utils.buildResourcePath(userName, projectName, schemaName, modelSchema, modelName)));
        }
        var newItem = {
            type: 'schemacontextmodel', 
            userName: null, 
            projectName: null, 
            schemaName: null, 
            modelSchema: null, 
            modelName: null
         };
        schemacontextmodelPersister.putSchemacontextmodel(newItem, function(err, result) {
            if (err) {
                return callback(err);
            }
            //
            // TODO add to oneTable ?
            // TODO or always make the oneTable api call this insert ?
            //
            callback(null, result);
        })
    })
}
;

.prototype.updateSchemacontextmodel = function(item, callback) {
    schemacontextmodelPersister.put(item, callback)
}

.prototype.deleteSchemacontextmodel = function(item, callback) {
    schemacontextmodelPersister.delete(item, callback)
}

