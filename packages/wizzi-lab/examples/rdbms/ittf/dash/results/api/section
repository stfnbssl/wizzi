/*
    artifact generator: C:\My\wizzi\stfnbssl\wizzi\node_modules\wizzi-js\lib\artifacts\js\module\gen\main.js
    package: wizzi-js@0.7.8
    primary source IttfDocument: C:\My\wizzi\stfnbssl\wizzi\packages\wizzi-lab\lib\artifacts\rdbms\jsonmodel\gen\ittf\jsonmodel.js.ittf
    utc time: Sat, 30 Jul 2022 16:00:35 GMT
*/
'use strict';

//

var sectionPersister = require('../persisters/section');

var md = module.exports = {};

md.getSections = function(userName, projectName, sectionName, callback) {
    sectionPersister.getSections(userName, projectName, sectionName, callback)
}
;

md.getSection = function(userName, projectName, sectionName, callback) {
    
    // Persister.get must give back a NotFoundError if item does not exists
    
    sectionPersister.get(userName, projectName, sectionName, callback)
}
;

md.insertSection = function(userName, projectName, sectionName, callback) {
    sectionPersister.exists(userName, projectName, sectionName, function(err, result) {
        if (err) {
            return callback(err);
        }
        if (result) {
            return callback(new errors.AlreadyExistsError('section', utils.buildResourcePath(userName, projectName, sectionName)));
        }
        var newItem = {
            type: 'section', 
            userName: null, 
            projectName: null, 
            sectionName: null, 
            sectionModels: [], 
            sectionFragments: []
         };
        sectionPersister.putSection(newItem, function(err, result) {
            if (err) {
                return callback(err);
            }
            //
            // TODO add to oneTable ?
            // TODO or always make the oneTable api call this insert ?
            //
            callback(null, result);
        })
    })
}
;

.prototype.updateSection = function(item, callback) {
    sectionPersister.put(item, callback)
}

.prototype.deleteSection = function(item, callback) {
    sectionPersister.delete(item, callback)
}

// TODO r.manyTable().keyColumns() - table.keyColumns()
md.getSectionSectionmodel = function(userName, projectName, sectionName, modelSchema, modelName, callback) {
    
    md.getSection(userName, projectName, sectionName, function(err, result) {
        if (err) {
            return callback(err);
        }
        var ret = util.aFind(result.sectionModels, {
            'userName': userName, 
            'projectName': projectName, 
            'sectionName': sectionName, 
            'modelSchema': modelSchema, 
            'modelName': modelName
         });
        if (ret) {
            return callback(null, ret);
        }
        else {
            return callback(new errors.NotFoundError(utils.buildResourceName('section', 'sectionmodel'), utils.buildResourcePath(userName, projectName, sectionName, modelSchema, modelName)));
        }
    })
}
;

md.addSectionSectionmodel = function(userName, projectName, sectionName, modelSchema, modelName, callback) {
    
    md.getSection(userName, projectName, sectionName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('section', 'sectionmodel'), utils.buildResourcePath(userName, projectName, sectionName, modelSchema, modelName)));
        }
        
        _.addFragment(result, fragmentSchema, fragmentName)
        
        persisterSection.put(userName, projectName, sectionName, result, callback)
        
    })
}
;

md.removeSectionSectionmodel = function(userName, projectName, sectionName, modelSchema, modelName, callback) {
    
    md.getSection(userName, projectName, sectionName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('section', 'sectionmodel'), utils.buildResourcePath(userName, projectName, sectionName, modelSchema, modelName)));
        }
        
        _.removeFragment(result, fragmentSchema, fragmentName)
        
        persisterSection.put(userName, projectName, sectionName, result, callback)
        
    })
}
;

// TODO r.manyTable().keyColumns() - table.keyColumns()
md.getSectionSectionfragment = function(userName, projectName, sectionName, fragmentName, callback) {
    
    md.getSection(userName, projectName, sectionName, function(err, result) {
        if (err) {
            return callback(err);
        }
        var ret = util.aFind(result.sectionFragments, {
            'userName': userName, 
            'projectName': projectName, 
            'sectionName': sectionName, 
            'fragmentName': fragmentName
         });
        if (ret) {
            return callback(null, ret);
        }
        else {
            return callback(new errors.NotFoundError(utils.buildResourceName('section', 'sectionfragment'), utils.buildResourcePath(userName, projectName, sectionName, fragmentName)));
        }
    })
}
;

md.addSectionSectionfragment = function(userName, projectName, sectionName, fragmentName, callback) {
    
    md.getSection(userName, projectName, sectionName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('section', 'sectionfragment'), utils.buildResourcePath(userName, projectName, sectionName, fragmentName)));
        }
        
        _.addFragment(result, fragmentSchema, fragmentName)
        
        persisterSection.put(userName, projectName, sectionName, result, callback)
        
    })
}
;

md.removeSectionSectionfragment = function(userName, projectName, sectionName, fragmentName, callback) {
    
    md.getSection(userName, projectName, sectionName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('section', 'sectionfragment'), utils.buildResourcePath(userName, projectName, sectionName, fragmentName)));
        }
        
        _.removeFragment(result, fragmentSchema, fragmentName)
        
        persisterSection.put(userName, projectName, sectionName, result, callback)
        
    })
}
;

