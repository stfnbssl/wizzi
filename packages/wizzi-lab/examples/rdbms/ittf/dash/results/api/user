/*
    artifact generator: C:\My\wizzi\stfnbssl\wizzi\node_modules\wizzi-js\lib\artifacts\js\module\gen\main.js
    package: wizzi-js@0.7.8
    primary source IttfDocument: C:\My\wizzi\stfnbssl\wizzi\packages\wizzi-lab\lib\artifacts\rdbms\jsonmodel\gen\ittf\jsonmodel.js.ittf
    utc time: Sat, 30 Jul 2022 16:00:35 GMT
*/
'use strict';

//

var userPersister = require('../persisters/user');

var md = module.exports = {};

md.getUsers = function(userName, callback) {
    userPersister.getUsers(userName, callback)
}
;

md.getUser = function(userName, callback) {
    
    // Persister.get must give back a NotFoundError if item does not exists
    
    userPersister.get(userName, callback)
}
;

md.insertUser = function(userName, callback) {
    userPersister.exists(userName, function(err, result) {
        if (err) {
            return callback(err);
        }
        if (result) {
            return callback(new errors.AlreadyExistsError('user', utils.buildResourcePath(userName)));
        }
        var newItem = {
            type: 'user', 
            userName: null, 
            projects: []
         };
        userPersister.putUser(newItem, function(err, result) {
            if (err) {
                return callback(err);
            }
            //
            // TODO add to oneTable ?
            // TODO or always make the oneTable api call this insert ?
            //
            callback(null, result);
        })
    })
}
;

.prototype.updateUser = function(item, callback) {
    userPersister.put(item, callback)
}

.prototype.deleteUser = function(item, callback) {
    userPersister.delete(item, callback)
}

// TODO r.manyTable().keyColumns() - table.keyColumns()
md.getUserProject = function(userName, projectName, callback) {
    
    md.getUser(userName, function(err, result) {
        if (err) {
            return callback(err);
        }
        var ret = util.aFind(result.projects, {
            'userName': userName, 
            'projectName': projectName
         });
        if (ret) {
            return callback(null, ret);
        }
        else {
            return callback(new errors.NotFoundError(utils.buildResourceName('user', 'project'), utils.buildResourcePath(userName, projectName)));
        }
    })
}
;

md.addUserProject = function(userName, projectName, callback) {
    
    md.getUser(userName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('user', 'project'), utils.buildResourcePath(userName, projectName)));
        }
        
        _.addFragment(result, fragmentSchema, fragmentName)
        
        persisterUser.put(userName, result, callback)
        
    })
}
;

md.removeUserProject = function(userName, projectName, callback) {
    
    md.getUser(userName, function(err, result) {
        if (err) {
            return callback(err);
        }
        
        if (!result) {
            return callback(new errors.NotFoundError(utils.buildResourceName('user', 'project'), utils.buildResourcePath(userName, projectName)));
        }
        
        _.removeFragment(result, fragmentSchema, fragmentName)
        
        persisterUser.put(userName, result, callback)
        
    })
}
;

