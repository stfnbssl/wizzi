module
	kind jsfile
	var cr = require('../../lib/util/jsCodeReplacer')

	var code 
		[
			@ 'var x = 1;'
			@ '{{alfa}}'
			@ 'var y = 2;'
			._ join('\n')

	info 'code', code
	var r = cr.clean(code)
	info 'cr.clean', r
	info 'cr.restore', cr.restore(r.codeCleaned, r.replaceds)