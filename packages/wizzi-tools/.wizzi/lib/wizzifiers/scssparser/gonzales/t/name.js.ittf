$group
	$params name, type|none

	# loog 't/name.node.${name}, value: ', node.${name}, ', type: string'
	if typeof node.${name} !== 'undefined'
		set ret.name = node.${name}.toString()
		set ret.textified = ret.name
	# loog 't/name ittf.ret', ret
	
