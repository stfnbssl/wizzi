$group
    name "${metaCtx.pkgName}"
    pluginBasename "${verify.isUndefined(pluginBasename) ? metaCtx.pkgName : pluginBasename}"
    npnName "${verify.isUndefined(pluginBasename) ? metaCtx.pkgName : "@wizzi/plugin." + pluginBasename}"
    description "${metaCtx.pkgName}"
    version "0.0.1"
    { author
        name "Stefano Bassoli"
        email "stfn.bssl@gmail.com"
    { license
        name "MIT"
        copy "copy 2023"
    { github
        userid "stfnbssl"

